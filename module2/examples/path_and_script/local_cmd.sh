#!/usr/bin/env bash
# Module 2 path_and_script example: run with ./local_cmd.sh (not "local_cmd.sh")
echo "This script ran because you used ./local_cmd.sh"
echo "The current directory is not in PATH, so 'local_cmd.sh' alone would be 'command not found'"

# Module 7 submodules — commands to try

# This repo has a demo submodule at module7/examples/submodules/demo_repo (octocat/Hello-World).
# After cloning learn_unix_git, populate it with:
git submodule update --init --recursive
# or: ./scripts/git-submodule-init.sh
# Then: ls module7/examples/submodules/demo_repo && cat module7/examples/submodules/demo_repo/README

# Add a submodule (replace URL and path with a real repo)
# git submodule add https://github.com/some-org/example-repo.git external/example
# git add .gitmodules external/example
# git commit -m "Add example as submodule"

# After cloning a repo that has submodules:
git submodule update --init --recursive

# Or clone with submodules in one step:
# git clone --recurse-submodules <url>

# Update submodule to latest and record in parent:
# cd external/example
# git pull
# cd ../..
# git add external/example
# git commit -m "Update submodule to latest"

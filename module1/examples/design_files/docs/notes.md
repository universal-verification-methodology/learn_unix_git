# Design notes (sample)

This is a **Markdown** file. In real projects you might have:

- Design specs
- Meeting notes
- README files

Open from the shell with:

```bash
cat notes.md
# or
less notes.md
```

Same idea for `.v`, `.sv`, and `.cpp` in `src/`, `tb/`, and `sim/`.
